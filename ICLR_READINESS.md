# ICLR readiness — September 17, 2026

**CONDITIONAL GO for a narrowly supported shared-latent encoder paper; NO-GO today for “bidirectional encoders beat causal policies across benchmarks” or “best method.”** Set a firm scientific decision gate on September 21. Ten working days below are September 17–26, with September 27 as the user’s submission target; verify the actual conference/abstract deadlines separately.

**Schedule revision after auditing the LayerwiseFM topology:** the released77.0% Qwen3-PI model used the legacy all-cross path that [issue #372](https://github.com/starVLA/starVLA/issues/372) shows has no action-token self-attention or direct state-to-action path. Its score is a valid benchmark observation but not an architecture to adopt: our DROID trials exposed noisy within-chunk actions. The matched QwenPI_v4 dual-attention run gives every block its corresponding VLM layer while action/state tokens interact. The prematurely queued full run1857258 and evals1857259/60 were cancelled before start; v4 now passes a20-update, in-training-eval gate at the exact full-run batch shape before full training is submitted. Legacy all-cross remains reproduction-only. Camera routing waits behind the v4 and shared-z+GR00T gates. The released baseline still requires exact4k evaluation because it is4B/100k/full-finetune/horizon8 over all10,030 tasks, while our causal model is2B/20k and our77.375% shared-z mean uses a different4k subset. See the [configuration/protocol audit](UPSTREAM_LIBERO_PLUS_AUDIT.md).

What is sufficient: internal LIBERO-plus exact 4k results for causal seeds 42/43/44 (71.658% mean, 0.775 pp SD), shared-z v5 seeds 42/43 (77.375%, 1.273 pp SD), v5-PI and v5-GR00T seeds 42/43. Shared-z’s matched-seed gain is +5.375 pp. Raw counts, all sibling directories, failures and canonical choices are audited. RoboCasa has both training seeds 4/42 complete over the declared 17 tasks, 48 episodes/task: causal 35.539% versus encoder 37.806%, pooled n 1,632/arm; +2.267±1.687 pp is below two combined episode SE. State-corruption 18 cells and no-state 6 cells have landed, but cover only 3 tasks/one seed.

What blocks the broad claim: causal+shared-z is missing; encoder/pretraining/head changes are bundled, and shared-z uses global batch32 versus causal64 with different head dropout. Plain LIBERO mostly has400episodes. RoboCasa omits one atomic and32composite tasks. CoT82.6/82.2% remains n1,153 with stale roots; jobs1850350–52/1855180 and shared-z-GR00T1851382→1851383/84 were unfinished at audit. MLM-control loss is disabled: do not claim an objective gain. State-format controls and a third behavior domain remain missing. No probe proxy is validated.

The strongest external reference verified is InternVLA-A1.5:98.9% LIBERO and 84.8% zero-shot LIBERO-plus ([released model](https://huggingface.co/InternRobotics/InternVLA-A1.5-Libero)); our 77.375% is not a matched comparison. Official RoboCasa365 leader Xiaomi-Robotics-1 reports 57.4% overall/80.2% atomic ([leaderboard](https://robocasa.ai/leaderboard.html)). Neither competitor nor any other published number is recomputed from our files.

Every new training configuration uses the same gate: two runs share one4-GPU node, each run uses2GPUs and the real per-device batch (32 here, global64/run), completes20 optimizer updates, and executes the normal in-training validation step. Inspect finite train/eval losses and the saved step20 checkpoint; only then submit full training. This is a data/model integration test, not a simulator rollout. Compute is not the limiting resource, so independent gated tracks run in parallel rather than waiting for the headline track.

| day | headline LIBERO-plus track | parallel nonblocking track |
|---|---|---|
|1 · Sep 17 | Run and inspect the QwenPI_v4 20-step/two-seed smoke at full batch shape; submit full seeds42/43 immediately only if it passes. Let shared-z+GR00T1851382 and its dependent evals finish. | Prepare released PI/OFT exact-protocol reproduction. Port v4 into the RoboCasa setup and run its own20-step/in-training-eval smoke before any full RoboCasa training. Keep causal+shared-z implementation moving. |
|2 · Sep 18 | Monitor v4 training; queue exact4k evals only after20k checkpoints exist. Compare both seeds with alternating v5 controls and inspect action-difference/jerk statistics. Read GR00T+z. | If the RoboCasa smoke passes, start matched v4 seeds4/42 in parallel with existing controls. Start upstream PI exact-task evaluation; OFT follows only after its interface smoke passes. |
|3 · Sep 19 | Promote v4 only for≥1pp mean rollout gain without seed instability or worse temporal diagnostics. Launch claim-critical causal+shared-z controls after their20-step gate. | Continue RoboCasa v4; prepare state/no-state matched configs. Give one-camera-one-z only a20-step screen if v4/GR00T evidence still points to a camera bottleneck. |
|4 · Sep 20 | Drop v4 if it misses its gate. Audit4k versus10,030-task protocol effects and begin writing from reproducible comparisons. | Run state-format controls and matched-compute accounting. Promote camera routing only for a clear validation signal and feasible benchmark confirmation. |
|5 · Sep 21 | Scientific gate: encoder contribution must survive matched causal+z. Freeze the headline recipe and launch final seed confirmations. | Decide whether RoboCasa is positive or neutral from matched full results. Stop weak architecture branches; retain only independent jobs that answer a paper claim. |
|6 · Sep 22 | Complete claim-critical ablations, external baselines and compute/latency table. MLM-loss on/off only if claiming that loss. | Finish RoboCasa state/no-state or backbone controls and external protocol matching. Draft methods, limitations and appendix in parallel. |
|7 · Sep 23 | All main-seed evals should finish. Recount suites/categories, uncertainty and checkpoint provenance. | Reconcile failed-unit/task manifests and freeze RoboCasa tables; complete figures and full draft. |
|8 · Sep 24 | Last safe arrival for headline results; freeze machine-readable tables and manuscript claims. | Only isolated corrections or appendix results may land after this point. Run reproducibility audit. |
|9 · Sep 25 | Skeptical internal review: causal attribution, comparison fairness, novelty and reproducibility. | Fix writing, figures, references and anonymized artifact instructions; no exploratory training. |
|10 · Sep 26 | Final proofread, anonymization, code/artifact links, PDF rendering and submission buffer for Sep27. | Re-run table-generation/checksum scripts only; new experiments require a documented correctness issue. |

Parallel execution assumes4–6available4-GPU nodes; queue capacity is unverified. Healthy4k LIBERO-plus evals took1h28m06s/1h38m38s (1850294/1836354): reserve2h and6–8GPU-h/run. CoT may need8–12h plus retries. A fresh RoboCasa paired17-task sweep took57min (1842871,22:44:36→23:41:32;1,632rollouts): reserve1.5–2h/pair. The42min seed42 retry was partial. LIBERO seed-pair training took7.5h/4GPUs; budget8–10h and~30GPU-h for v4 because every block attends to a longer fused key/value sequence. Its two exact4k evaluations add12–16GPU-h. RoboCasa pairs took12h+7.6h resume. Camera screen plus confirmation remains55–115GPU-h,2–3days plus integration/queue and starts only after v4/GR00T+z selection. On constrained capacity, drop camera work before attribution controls. Official50-task training costs remain unverified.

Honest fallback: “A supervised shared-latent pathway improves geometry/initial-state robustness of encoder-based policies on LIBERO-plus, with neutral mean success on RoboCasa.” State robustness needs format controls; sample efficiency needs matched learning curves. Representation quality cannot carry this encoder paper. A narrower mechanism-backed control result could be publishable: [Cosmos Policy, ICLR2026, and hand-view sensing, ICLR2022](https://moojink.com/) are relevant precedents, not acceptance predictions. If causal+z removes the encoder-specific gain and no second domain improves by Sep21, do not submit the broad thesis unchanged.

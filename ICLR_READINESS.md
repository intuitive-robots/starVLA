# ICLR readiness — September 17, 2026

**September 17 daily-schedule correction:** retained-memory shared z already has old-backbone PI exact4k results: partial dropout0.15=78.125% (3,125/4,000), always retained=74.875% (2,995/4,000), z-only=76.600% (3,064/4,000), each seed42. This is not a missing generic ablation; replication on v5+GR00T is missing. Add a nonblocking Day1/2 dropout0.15 and1 seed42/43 smoke pairs (two GPUs/run,32/device,20 updates plus trainer validation); after it passes, train and chain exact4k evaluation without delaying causal+z or causal trace-use controls. Train both arms at global batch64; the historical batch32 dropout1 runs are a separate batch comparison. Batch size is a confound in the old z versus no-z comparison, and larger batch is not presumed better. Day3/4: assess both seeds before choosing the retained-memory recipe; do not change the headline or the Sep21 scientific gate on a single-seed PI difference. These are scheduled tasks, not newly submitted jobs. See the raw counts and uncertainty in ENCODER_PUSH_PLAN.md.

**CONDITIONAL GO for a narrowly supported shared-latent encoder paper; NO-GO today for “bidirectional encoders beat causal policies across benchmarks” or “best method.”** The causal explicit-CoT `ours_v3_cotw01` run has now finished at83.075% over4,000 episodes,4.600pp above the78.475% two-seed shared-z+GR00T mean. The broad thesis currently fails on the best internal LIBERO-plus result. It becomes viable only if causal+shared-z establishes an encoder-specific interaction and/or tied two-pass bidirectional trace re-encoding closes this gap. Set a firm scientific decision gate on September21. Ten working days below are September17–26, with September27 as the user’s submission target; verify the actual conference/abstract deadlines separately.

**Schedule revision after auditing the LayerwiseFM topology, auxiliary objectives and causal CoT path:** the released77.0% Qwen3-PI model used the legacy all-cross path that [issue #372](https://github.com/starVLA/starVLA/issues/372) shows has no action-token self-attention or direct state-to-action path. Its score is a valid benchmark observation but not an architecture to adopt: our DROID trials exposed noisy within-chunk actions. The matched QwenPI_v4 dual-attention run gives every block its corresponding VLM layer while action/state tokens interact. The corrected v4 pair1858694 is running after its20-update gate, with evals1858835/36 chained. Legacy all-cross remains reproduction-only. Dense, trace, encoder FAST-readout, MLM and decoder-CoT auxiliaries did not make their decoded prediction a rollout-visible action input; only shared z currently provides such a persistent encoder interface. The causal CoT arm instead generates a trace, re-encodes prompt plus trace, and makes GR00T consume the resulting contextual states. The first encoder analogue now omits z: predict five semantic coordinates in pass1, insert only their rounded embeddings into a tied bidirectional pass2, and give GR00T all pass2 states. This cleanly tests re-encoding against one-pass and two-pass dummy-trace controls. Pass2 z plus contextual trace states is the performance follow-up. Direct coordinate tokens are the cheaper ablation. Verify causal trace use with prompt-only and wrong-trace inference before full training. Camera routing and further auxiliary-head sweeps wait behind causal+shared-z, this test and v4. See the [configuration/protocol audit](UPSTREAM_LIBERO_PLUS_AUDIT.md).

What is sufficient: internal LIBERO-plus exact 4k results for causal seeds42/43/44 (71.658% mean,0.775pp SD), shared-z v5 PI seeds42/43 (77.375%,1.273pp SD), and shared-z v5 GR00T seeds42/43 (78.475%,0.283pp SD). Shared-z+GR00T is the strongest completed two-seed internal arm: +3.850pp over matched v5-GR00T and +6.817pp over the internal causal PI baseline, although the latter changes both latent path and head. Shared z adds4.250pp in the matched PI comparison. Raw counts, all sibling directories, failures and canonical choices are audited. RoboCasa has both training seeds4/42 complete over the declared17 tasks,48 episodes/task: causal35.539% versus encoder37.806%, pooled n1,632/arm; +2.267±1.687pp is below two combined episode SE. State-corruption18 cells and no-state6 cells have landed, but cover only3 tasks/one seed.

What blocks the broad claim: causal+shared-z is missing; encoder/pretraining/head changes are bundled, and shared-z uses global batch32 versus causal64 with different head dropout. The causal `ours_v3_cotw01` CoT root is now exact4k at83.075%; the detector-trace root independently reaches81.225%, and no encoder arm matches either. Each is one training seed, and prompt-only/wrong-trace controls must establish whether aligned trace content causes the gain. Plain LIBERO mostly has400episodes. RoboCasa omits one atomic and32composite tasks. MLM-control loss is disabled: do not claim an objective gain. Encoder dense/readout/trace pretraining heads are not rollout-visible planning interfaces; the lower dense-backbone rollout also has a pretraining-history confound. State-format controls and a third behavior domain remain missing. No probe proxy is validated.

The strongest external reference verified is InternVLA-A1.5:98.9% LIBERO and84.8% zero-shot LIBERO-plus ([released model](https://huggingface.co/InternRobotics/InternVLA-A1.5-Libero)); our78.475% encoder mean and83.075% single-seed causal CoT are not matched comparisons. Official RoboCasa365 leader Xiaomi-Robotics-1 reports57.4% overall/80.2% atomic ([leaderboard](https://robocasa.ai/leaderboard.html)). Neither competitor nor any other published number is recomputed from our files.

Every new training configuration uses the same gate: two runs share one4-GPU node, each run uses2GPUs and the experiment's real per-device batch (32/device, global64/run for both Track M arms), completes20 optimizer updates, and executes the normal in-training validation step. Inspect finite train/eval losses and the saved step20 checkpoint; only then submit full training. This is a data/model integration test, not a simulator rollout. Compute is not the limiting resource, so independent gated tracks run in parallel rather than waiting for the headline track.

| day | headline LIBERO-plus track | parallel nonblocking track |
|---|---|---|
|1 · Sep 17 | Corrected QwenPI_v4 LIBERO smoke passed for seeds42/43; full pair1858694 is running and exact4k evals1858835/36 are chained. Shared-z+GR00T exact4k seeds completed at78.675%/78.275%. Causal CoT roots completed at83.075% (`ours`,3,323/4,000) and81.225% (`det`,3,249/4,000); resume1858474 finished with empty failure sentinels. Freeze configs and prepare paired prompt-only/wrong-trace controls on the83.075% checkpoint. | RoboCasa v4 smoke passed; full pair1858730 is running, continuation1858851 and17-task evals1858852/53 are chained. Finish causal+shared-z implementation. Prepare independent Track M (one-pass shared-z, memory dropout0.15 versus1) and Track T (no-z, predicted five-point trace, all pass2 states) smokes on separate nodes; neither waits for the other. Both Track M YAMLs are prepared at batch64: z-only control plus retained-memory candidate. The user selected the batch64 controlled design; smoke gates still precede full training. RoboCasa supervision generation and native loader integration completed:9,126 episodes/9,591 subtasks/2,231,347 frames across18 tasks, with full object/gripper paths, masked z targets and joint crop/rotation alignment.189 failures were repaired; final audit has no missing episodes. The465 multi-part episodes are included in the main recipe with explicit inferred-boundary provenance; masking them is a diagnostic ablation only. Port GR00T+z in parallel; see ROBOCASA_SUPERVISION_PLAN.md. Do not launch another auxiliary-only sweep. |
|2 · Sep 18 | Run clean/prompt-only/wrong-trace controls on the same CoT checkpoint and episodes. In parallel run the causal+shared-z GR00T two-seed20-update smoke. Implement tied two-pass encoder trace re-encoding without our fork's learned-query readout. Direct checkpoint shapes establish that real-robot `cot_trace_ours` used all-cross at horizon24 while its non-CoT GR00T control used canonical alternating cross/self; the83.075% causal LIBERO-plus CoT run is also all-cross. Use two4-GPU nodes for the decisive20-update trainer-eval2×2: canonical `traceloop_1pass_noz_all` versus `traceloop_2pass_noz_pred_all`, and all-cross `traceloop_1pass_noz_allcross_all` versus `traceloop_2pass_noz_pred_allcross_all`. Compare loop minus one-pass within each topology; do not select topology from architectural intuition. Then smoke null/oracle and `traceloop_2pass_z_pred_trace` using the better topology. Monitor action-difference/jerk statistics. | Continue RoboCasa v4 and start upstream PI exact-task evaluation. Native masks and crop/rotation alignment are integrated and tested on the real two-camera loader; existing runs in fact had no image augmentation despite the YAML field, so every new matched arm must explicitly enable `robocasa_joint_augmentation`. Port GR00T+z and prepare matched no-z/z+state/z+state+memory arms, preserving direct state. Use remaining object motion for a causal-matched trace test; keep full-subtask gripper motion as a separate ablation. Include the465 inferred-boundary episodes with provenance; reserve boundary masking for a diagnostic ablation. Complete the two-run20-update trainer-eval smoke at32/device before full training. If the model port/smoke is unresolved by end of Day2, remove supervised RoboCasa transfer from the Sep21 critical path. Track M proceeds independently on LIBERO-plus as scheduled. |
|3 · Sep 19 | Launch two no-z two-pass seeds first if causal trace content passes the≥1pp gate and the pass2 oracle shows≥2pp headroom. With compute available, launch `z_plus_trace` in parallel after its smoke rather than waiting for full no-z completion. Require finite trace/action losses, nonzero intended gradients and proof that no hidden continuous channel bypasses rounded coordinates; chain exact4k evals. Run the one-pass direct-token arm as the matched ablation. Promote v4 only for≥1pp mean gain without seed instability. | Continue RoboCasa v4; once RoboCasa label checks and20-update trainer-eval smokes pass, start independent matched GR00T pairs with chained evaluation (throughput to be measured). Keep autoregressive encoder-decoder trace and single-pass mid-layer loopback as follow-ups. Camera routing remains post-deadline. |
|4 · Sep 20 | Drop v4 or reasoning conditioning if either misses its gate. Audit4k versus10,030-task protocol effects and begin writing from reproducible comparisons. | Assess Track M against new batch64 z-only, compare that control with historical batch32 (fixed updates, different example budgets), and assess Track T against one-pass/no-z plus two-pass dummy-trace controls; do not attribute a gain over z-only alone to trace reasoning. Run state-format controls and matched-compute accounting. Keep camera routing post-deadline. |
|5 · Sep 21 | Scientific gate: encoder contribution must survive matched causal+z. Freeze the headline recipe and launch final seed confirmations. | Decide whether RoboCasa is positive or neutral from matched full results. Stop weak architecture branches; retain only independent jobs that answer a paper claim. |
|6 · Sep 22 | Complete claim-critical ablations, external baselines and compute/latency table. MLM-loss on/off only if claiming that loss. | Finish RoboCasa state/no-state or backbone controls and external protocol matching. Draft methods, limitations and appendix in parallel. |
|7 · Sep 23 | All main-seed evals should finish. Recount suites/categories, uncertainty and checkpoint provenance. | Reconcile failed-unit/task manifests and freeze RoboCasa tables; complete figures and full draft. |
|8 · Sep 24 | Last safe arrival for headline results; freeze machine-readable tables and manuscript claims. | Only isolated corrections or appendix results may land after this point. Run reproducibility audit. |
|9 · Sep 25 | Skeptical internal review: causal attribution, comparison fairness, novelty and reproducibility. | Fix writing, figures, references and anonymized artifact instructions; no exploratory training. |
|10 · Sep 26 | Final proofread, anonymization, code/artifact links, PDF rendering and submission buffer for Sep27. | Re-run table-generation/checksum scripts only; new experiments require a documented correctness issue. |

Parallel execution assumes4–6available4-GPU nodes; queue capacity is unverified. Healthy4k LIBERO-plus evals took1h28m06s/1h38m38s (1850294/1836354): reserve2h and6–8GPU-h/run. CoT may need8–12h plus retries. A fresh RoboCasa paired17-task sweep took57min (1842871,22:44:36→23:41:32;1,632rollouts): reserve1.5–2h/pair. The42min seed42 retry was partial. LIBERO seed-pair training took7.5h/4GPUs; budget8–10h and~30GPU-h for v4 because every block attends to a longer fused key/value sequence. Its two exact4k evaluations add12–16GPU-h. RoboCasa pairs took12h+7.6h resume. Camera screen plus confirmation remains55–115GPU-h,2–3days plus integration/queue and starts only after v4/GR00T+z selection. On constrained capacity, drop camera work before attribution controls. Official50-task training costs remain unverified.

Honest fallback: “A supervised shared-latent pathway improves geometry/initial-state robustness of encoder-based policies on LIBERO-plus, with neutral mean success on RoboCasa.” State robustness needs format controls; sample efficiency needs matched learning curves. Representation quality cannot carry this encoder paper. A narrower mechanism-backed control result could be publishable: [Cosmos Policy, ICLR2026, and hand-view sensing, ICLR2022](https://moojink.com/) are relevant precedents, not acceptance predictions. If causal+z removes the encoder-specific gain and no second domain improves by Sep21, do not submit the broad thesis unchanged.


September17 execution update: batch64 GR00T+z smokes 1861585, 1861586 submitted for dropout1 and0.15, each seeds42/43 on one4-GPU node.20 updates, validation at10/20, step20 checkpoint required. Full training and its exact4k eval dependencies will be submitted only after the smoke gate passes.


September17 launch update: both batch64 GR00T+z smokes1861585/86 passed (four runs,20 updates,eval10/20,complete step20 checkpoints). Full dropout1 seed pair1861658 and dropout0.15 pair1861661 submitted at20k steps,32/device,2GPUs/run. Exact4k evaluations 1861659, 1861660, 1861662, 1861663 are chained with afterok to their full pair. Pin evaluation to8workers/GPU,2servers/GPU co-located on all4GPUs, batch ceiling4 and zero wait; do not inherit newly changed throughput defaults. Steady smoke steps~1.8–2.1s suggest10–12h training plus startup/eval/checkpoint overhead;12h wall limit, so check progress before timeout and resume if necessary rather than treating a missing eval as zero.

Runtime audit correction: action-only encoder GR00T already used batch64 and measured1.024s/update (training-loop5h43m); the prior batch32 shared-z pair took5h55m04s at~1.045s/update. Shared-z adds a no-gradient future-image encoder pass for its temporal targets, plus auxiliary losses/regularizers. Both GR00T recipes use eight flow repeats, so repeats do not explain their difference. Smoke logging_frequency1 also invokes gradient diagnostics every update, unlike100 in production; do not extrapolate solely from smoke throughput. All four full batch64 runs passed20 updates: early ordinary-step medians~1.65–1.89s, implying~9.2–10.5h of updates plus validation/saving. This is an early estimate, not a profile attributing all overhead to the future pass. Retained memory and z-only have similar measured runtime.
